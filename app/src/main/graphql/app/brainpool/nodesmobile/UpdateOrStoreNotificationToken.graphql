mutation UpdateOrStoreNotificationToken ($notificationInput:NotificationInput!){
    updateOrStoreNotificationToken(data:$notificationInput) {
        success
        detail {
            code
            message
            type
        }
    }
}